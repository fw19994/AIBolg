# 测试环境构建：复制为 web/.env.test 后执行 npm run build:test
# （.env.test 通常不提交；CI 可在流水线里注入同名环境变量）
VITE_API_URL=https://api.shoppingfw.cn
# 与测试环境 Go 的 server.routerPrefix 一致；无项目前缀则留空或删除
VITE_ROUTER_PREFIX=inkmind
# 前端静态站若在 OSS 子路径（非域名根），与访问路径一致，如 /inkmind/；根路径部署可删除本行
# VITE_DEPLOY_BASE=/inkmind/
